# Dev: Vite dev server com HMR
FROM node:20-alpine

WORKDIR /app
COPY package*.json .
RUN npm install

# Código montado via bind mount no compose.dev

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
